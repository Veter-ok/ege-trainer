interface IMissedLatter {
    id: number
    title: string 
    task_id: number, 
    answer: string
    options: string[]
}

interface IStressInWords {
    id: number,
    word: string
    answer: string,
    options: string[]
}